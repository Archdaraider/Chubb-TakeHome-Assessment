spring:
  datasource:
    url: jdbc:h2:mem:claims-test;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

claims:
  outbox:
    relay:
      enabled: false
